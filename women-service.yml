server:
  port: 8082

spring:
  application:
    name: women-service

  datasource:
    url: jdbc:h2:mem:women_db
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
